name: Multi-Job Selenium Test Workflow

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  setup-node:
    name: 🟢 Setup Node.js
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Install dependencies
        working-directory: ./main-app
        run: npm install

  run-node:
    name: 🚀 Run Node.js App
    runs-on: ubuntu-latest
    needs: setup-node
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📦 Install dependencies again (new runner)
        working-directory: ./main-app
        run: npm install

      - name: 🟢 Start Node.js app in background
        working-directory: ./main-app
        run: |
          nohup npm start > app.log 2>&1 &
          sleep 10

  setup-selenium:
    name: 🧰 Setup Selenium
    runs-on: ubuntu-latest
    needs: run-node
    steps:
      - name: 🧱 Run Selenium in background
        run: |
          docker run -d -p 4444:4444 --shm-size=2g selenium/standalone-chrome:latest
          sleep 10

  run-tests:
    name: 🧪 Run Selenium Tests
    runs-on: ubuntu-latest
    needs: setup-selenium
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: ☕ Set up Java 22
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '22'

      - name: 🔧 Run Maven Selenium tests
        working-directory: ./selenium-cicd-tests
        run: mvn test
